#set the base url
server.servlet.context-path=/proiectcolectiv

# the url to database
spring.datasource.url=jdbc:sqlite:database.db
# dialect for sqlite used when running ProiectColectivApplication's main
spring.jpa.database-platform=com.proiect_colectiv.repository.dialect.SQLiteDialect
logging.level.root = info

security.basic.enabled=false
